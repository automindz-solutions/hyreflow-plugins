# PredictLeads Guidance

Use PredictLeads for company-level signals: hiring, technology detections, news events, financing events, connections, products, GitHub repositories, and similar companies.

Prefer direct company endpoints when you already have a domain — they bill a flat rate per request (0.8 credits) regardless of record count, up to 1,000 records on list endpoints. Discovery (`discover_*`) endpoints bill per returned result instead, so use them only when you need broad search across companies rather than a single known domain. Check `hyreflow tools get predictleads <method>` for the live rate on each.

Avoid follow/unfollow workflows for now. PredictLeads followed-company APIs are designed for webhook delivery and recurring monthly billing, not one-shot agent execution.

<!-- API-SURFACE:START (auto-generated by reference/_gen_api_surface.py — do not hand-edit) -->
## Callable surface
Call via the CLI: `hyreflow tools execute predictleads <method> --payload '{...}'` (preview with `--dry-run`; `hyreflow tools get predictleads <method>` returns the live contract + cost). Base: `https://predictleads.com/api/v3`. Any endpoint without a typed method is reachable through the tool's generic `request` passthrough.

- `api_subscription() -> Any` — GET /api_subscription — subscription status + monthly credit quota/used on the caller's own PredictLeads account; requires the workspace's own key.
- `company(identifier: str, **params) -> Any` — GET /companies/{domain_or_id} — one company (name, location, ticker, parent, similar). CONFIRMED.
- `connections(identifier: str, **params) -> Any` — GET /companies/{id}/connections — partner/vendor/integration/investor/parent connections.
- `discover_companies(**params) -> Any` — GET /discover/companies — discover companies by `location` + `sizes` (e.g. ['11-50','201-500']).
- `discover_financing_events(**params) -> Any` — GET /discover/financing_events — discover funding rounds by `financing_types_normalized` (e.g. ['series_a','series_a1']) + `company_location`, ordered by updated date. Per result. CONFIRMED.
- `discover_job_openings(**params) -> Any` — GET /discover/job_openings — discover openings by `onet_codes` (O*NET, e.g. '15-1252.00' software devs, '41-3091.00' sales) + `location`. Per result. Filters by O*NET occupation, NOT free-text title.
- `discover_news_events(**params) -> Any` — GET /discover/news_events — discover news events by `categories` + `company_location`. Per result. CONFIRMED.
- `discover_portfolio_companies(**params) -> Any` — GET /discover/portfolio_companies/connections — portfolio companies of VCs/accelerators (investor connections). Per result. Filters: first/last_seen_at_from/until. CONFIRMED.
- `discover_products(**params) -> Any` — GET /discover/products/latest — latest products by `sources` (['menu','pricing']) + date. Per result. CONFIRMED.
- `discover_startup_platform_posts(**params) -> Any` — GET /discover/startup_platform_posts — Hacker News Show/Job posts (hiring/launching). Filters: published_at_from/until, post_types (['show_hn','job_hn']). Per result. CONFIRMED.
- `discover_technology_detections(technology_id_or_fuzzy_name: str, **params) -> Any` — GET /discover/technologies/{tech_id_or_fuzzy_name}/technology_detections — companies using a specific technology (e.g. 'HubSpot'). Per result. CONFIRMED. Resolve the tech via technologies().
- `financing_events(identifier: str, **params) -> Any` — GET /companies/{id}/financing_events — a company's funding rounds (financing_type_normalized, amount_normalized, effective_date, investors). Filters: first_seen_at_from/until, page, limit. CONFIRMED.
- `follow_company(identifier: str, **params) -> Any` — POST /companies/{id}/follow — follow a company (webhook delivery). No PredictLeads credit.
- `followings(**params) -> Any` — GET /followings — companies you follow (for webhook delivery). 1 PredictLeads credit (vendor-side; hyreflow's price is this method's `cost` block). CONFIRMED.
- `get_extended_technology_detection(detection_id: str, **params) -> Any` — GET /extended_technology_detections/{id} — detection detail (how it was found). CONFIRMED.
- `get_job_opening(opening_id: str, **params) -> Any` — GET /job_openings/{id} — a single job opening by UUID. CONFIRMED.
- `get_news_event(event_id: str, **params) -> Any` — GET /news_events/{id} — a single news event by UUID. CONFIRMED.
- `get_product(product_id: str, **params) -> Any` — GET /products/{id} — a single product by UUID. CONFIRMED.
- `get_technology(id_or_fuzzy_name: str, **params) -> Any` — GET /technologies/{id_or_fuzzy_name} — a single technology by id or fuzzy name. CONFIRMED.
- `github_repositories(identifier: str, **params) -> Any` — GET /companies/{id}/github_repositories — a company's public repos + stars/forks timeseries. CONFIRMED.
- `iter_discover_financing_events(*, max_records: int = 1000, **params) -> Iterator[dict]` — Auto-paginate /discover/financing_events via `page` (bounded by max_records).
- `iter_discover_job_openings(*, max_records: int = 1000, **params) -> Iterator[dict]` — Auto-paginate /discover/job_openings via `page` (bounded by max_records).
- `job_openings(identifier: str, **params) -> Any` — GET /companies/{id}/job_openings — a company's roles. Filters: active_only, not_closed, first_seen_at_from/until, last_seen_at_from/until, with_description_only, with_location_only, categories (e.g. 'sales','software_development'), page, limit≤1000. CONFIRMED.
- `news_events(identifier: str, **params) -> Any` — GET /companies/{id}/news_events — a company's news events. Filters: found_at_from/until, categories (e.g. 'receives_financing','increases_headcount_by','hires'), page, limit. CONFIRMED.
- `products(identifier: str, **params) -> Any` — GET /companies/{id}/products — products/services/solutions detected for a company. CONFIRMED.
- `similar_companies(identifier: str, **params) -> Any` — GET /companies/{id}/similar_companies — lookalikes (score, reason; top 20). limit≤50. CONFIRMED.
- `technologies(**params) -> Any` — GET /technologies — catalog of ~54k tracked technologies (pricing/spend, categories). Filters: fuzzy_name, order_by. **3 PredictLeads credits/record** (vendor-side; the hyreflow price you budget with is this method's `cost` block). CONFIRMED.
- `technology_detections(identifier: str, **params) -> Any` — GET /companies/{id}/technology_detections — a company's tech stack. Filters: first/last_seen_at_from/until. CONFIRMED.
- `unfollow_company(identifier: str, **params) -> Any` — POST /companies/{id}/unfollow — unfollow a company. 1 PredictLeads credit (vendor-side; hyreflow's price is this method's `cost` block). CONFIRMED.
- `website_evolution(identifier: str, **params) -> Any` — GET /companies/{id}/website_evolution — subpages added over time (careers, blog…) = growth signal. CONFIRMED.

<!-- API-SURFACE:END -->
